# 🛠️ دليل استكشاف الأخطاء وإصلاحها - Fortress v3.1

## ❓ مشاكل شائعة

### 1. فشل اتصال WireGuard
**الأعراض**: لا يوجد إنترنت، سكريبت الفحص يعطي "WireGuard Down".
**الحل**:
- تأكد من صحة المفاتيح (Keys) في ملفات التكوين.
- تحقق من أن المنفذ `51820` (أو المنفذ المختار) مفتوح في جدار الحماية (Firewall).

### 2. بطء شديد في التصفح
**الأعراض**: المواقع تستغرق وقتاً طويلاً للتحميل.
**الحل**:
- هذا متوقع بسبب مرور البيانات عبر 3 قفزات في Tor.
- جرب تغيير الهوية (Identity Rotation) يدوياً: `sudo kill -HUP $(pidof tor)`.
- تأكد من تفعيل TCP BBR على الخوادم.

### 3. فشل ECMP في توزيع الحمل
**الأعراض**: كل حركة المرور تخرج من VPS واحد فقط.
**الحل**:
- تحقق من مسارات التوجيه: `ip route show`.
- تأكد من عمل `ecmp-daemon.py` في الخلفية.

### 4. حظر Tor من قبل مزود الخدمة
**الأعراض**: تعذر الاتصال بشبكة Tor نهائياً.
**الحل**:
- تأكد من تفعيل **obfs4** و **udp2raw** لإخفاء بصمة الاتصال.

## ログ (Logs)
لفحص السجلات التفصيلية:
- **WireGuard**: `sudo dmesg | grep wireguard`# 🛠️ دليل استكشاف الأخطاء وإصلاحها - Fortress v3.1

## ❓ مشاكل شائعة

### 1. فشل اتصال WireGuard
**الأعراض**: لا يوجد إنترنت، سكريبت الفحص يعطي "WireGuard Down".
**الحل**:
- تأكد من صحة المفاتيح (Keys) في ملفات التكوين.
- تحقق من أن المنفذ `51820` (أو المنفذ المختار) مفتوح في جدار الحماية (Firewall).

### 2. بطء شديد في التصفح
**الأعراض**: المواقع تستغرق وقتاً طويلاً للتحميل.
**الحل**:
- هذا متوقع بسبب مرور البيانات عبر 3 قفزات في Tor.
- جرب تغيير الهوية (Identity Rotation) يدوياً: `sudo kill -HUP $(pidof tor)`.
- تأكد من تفعيل TCP BBR على الخوادم.

### 3. فشل ECMP في توزيع الحمل
**الأعراض**: كل حركة المرور تخرج من VPS واحد فقط.
**الحل**:
- تحقق من مسارات التوجيه: `ip route show`.
- تأكد من عمل `ecmp-daemon.py` في الخلفية.

### 4. حظر Tor من قبل مزود الخدمة
**الأعراض**: تعذر الاتصال بشبكة Tor نهائياً.
**الحل**:
- تأكد من تفعيل **obfs4** و **udp2raw** لإخفاء بصمة الاتصال.

## ログ (Logs)
لفحص السجلات التفصيلية:
- **WireGuard**: `sudo dmesg | grep wireguard`
- **Tor**: `sudo tail -f /var/log/tor/log`
- **System**: `journalctl -u fortress-service`

---
*للمزيد من الدعم، قم بفتح Issue في المستودع.*
- **Tor**: `sudo tail -f /var/log/tor/log`
- **System**: `journalctl -u fortress-service`

---
*للمزيد من الدعم، قم بفتح Issue في المستودع.*
