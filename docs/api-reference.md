# 🔌 مرجع واجهة برمجة التطبيقات (API Reference) - Fortress v3.1

يحتوي النظام على 21 نقطة برمجية (API Endpoints) مخصصة للإدارة والتحكم والمراقبة.

## 🔑 المصادقة
تتطلب كافة الطلبات وجود رأس `Authorization`:
```http
Authorization: Bearer <YOUR_TOKEN>
```

## 📍 النقاط البرمجية الأساسية

### 1. إدارة الأنفاق (Tunnel Management)
- **GET** `/api/v1/tunnels`: عرض كافة الأنفاق النشطة.
- **POST** `/api/v1/tunnels/rotate`: تدوير الهوية يدوياً.

### 2. حالة الخوادم (VPS Status)
- **GET** `/api/v1/status`: الحصول على حالة كافة الطبقات (1-3 + Clean Exit).
- **GET** `/api/v1/metrics`: جلب بيانات Prometheus.

### 3. التحكم في الأمان (Security Control)
- **POST** `/api/v1/firewall/lockdown`: تفعيل وضع الإغلاق الشامل.
- **GET** `/api/v1/honeypot/alerts`: عرض تنبيهات نظام التمويه.

## 📊 نماذج الاستجابة
```json
{
  "status": "success",
  "data": {
    "active_vps": 3,
    "tor_status": "connected",
    "encryption": "hybrid-mlkem"
  }
}
```

---
*راجع تقرير اختبار النظام (System Test Report) لمزيد من التفاصيل حول نتائج كل نقطة.*
