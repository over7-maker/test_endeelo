# 🚀 دليل التثبيت - Fortress v3.1

## 📋 المتطلبات النظامية
- نظام تشغيل **Ubuntu 22.04 LTS**.
- صلاحيات الجذر (Root).
- 3-4 خوادم VPS (حسب التوزيع المختار).

## 🛠️ خطوات التثبيت بالتفصيل

### 1. إعداد خوادم VPS (الطبقات 1-3)
يجب تنفيذ الخطوات التالية على كل خادم VPS في الطبقة الأولى والثانية والثالثة:
```bash
cd scripts/vps-layer1/  # كرر للطبقات الأخرى
sudo bash install.sh
```

### 2. إعداد خادم الخروج النظيف (Clean Exit)
هذا الخادم هو المسؤول عن الخروج النهائي للإنترنت:
```bash
cd scripts/clean-exit/
sudo bash install.sh
```

### 3. إعداد البوابة المحلية (Local Gateway)
النقطة التي يتصل بها المستخدم:
```bash
cd scripts/local-gateway/
sudo bash install.sh
```

### 4. التحقق من الاتصال
بعد التثبيت، استخدم أداة المراقبة للتأكد من أن الأنفاق تعمل:
```bash
bash monitoring/check-status.sh
```

---
*ملاحظة: تأكد من توزيع مفاتيح SSH و WireGuard بشكل صحيح عبر الملفات قبل البدء.*
