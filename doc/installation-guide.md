# 🚀 دليل التثبيت التفصيلي - Fortress v3.1

يحتوي هذا الدليل على خطوات التثبيت اللازمة لإعداد بنية تحتية كاملة لنظام Fortress v3.1.

## 📋 المتطلبات النظامية
- خوادم تعمل بنظام **Ubuntu 22.04 LTS**.
- صلاحيات **Root** أو **Sudo**.
- اتصال بالإنترنت في جميع الخوادم.

## 🛠️ الخطوات الترتيبية للتثبيت

### الخطوة 1: إعداد خوادم التوجيه (VPS Layers 1-3)
يجب تنفيذ هذه الخطوات على كل خادم VPS مخصص للطبقات الثلاث الأولى:
1. الدخول إلى المجلد الخاص بالطبقة:
   ```bash
   cd scripts/vps-layer1/  # كرر للطبقة 2 و 3
   ```
2. تشغيل سكريبت التثبيت:
   ```bash
   sudo bash install.sh
   ```

### الخطوة 2: إعداد خادم الخروج النظيف (Clean Exit)
هذا الخادم هو المسؤول عن الخروج النهائي للإنترنت:
1. الانتقال للمجلد:
   ```bash
   cd scripts/clean-exit/
   ```
2. تشغيل التثبيت:
   ```bash
   sudo bash install.sh
   ```

### الخطوة 3: إعداد البوابة المحلية (Local Gateway)
يتم التثبيت على الجهاز المحلي أو الخادم المنزلي:
1. الانتقال للمجلد:
   ```bash
   cd scripts/local-gateway/
   ```
2. تشغيل التثبيت:
   ```bash
   sudo bash install.sh
   ```

### الخطوة 4: تفعيل نظام المراقبة
للتأكد من عمل جميع الأنفاق بشكل صحيح:
1. الانتقال للمجلد:
   ```bash
   cd monitoring/
   ```
2. تشغيل سكريبت الإعداد:
   ```bash
   sudo bash setup-monitoring.sh
   ```

## 🔍 التحقق من التثبيت
بعد الانتهاء من جميع الخطوات، يمكنك تشغيل أداة التحقق:
```bash
bash monitoring/check-status.sh
```

---
*تنبيه: تأكد من إعداد مفاتيح WireGuard بشكل صحيح في ملفات التكوين قبل البدء.*
